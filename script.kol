let add = fun(a, b) {
    // Adds two numbers
    a /*First*/+ b /*Second*/
}

println(add(5, 3));

let newAdder = fun(a) {
    let adder = fun(b) { a + b; };
    return adder;
};
let addTwo = newAdder(2);
println(addTwo(3));
